export interface ICollection {
  collection_id: string;
  create_at: string;
  image: string;
  name: string;
  popularity: number;
  volumes: number;
  volumes_total: number;
  __typename: string;
}
